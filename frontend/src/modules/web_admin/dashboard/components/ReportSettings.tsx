import EmptyPlaceholder from "./EmptyPlaceholder";

export default function ReportSettings() {
  return (
    <EmptyPlaceholder
      title="Data Configuration"
      description="Use this tab to define default filters, reporting timeframes, target departments, tracked statuses, and report data synchronization rules."
    />
  );
}