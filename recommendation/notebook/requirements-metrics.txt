# Minimal deps for Embedding metrics notebooks (04, 06).
# Use project root venv:  python -m venv .venv && source .venv/bin/activate
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
openpyxl>=3.1
ipykernel>=6.25
